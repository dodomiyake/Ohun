export interface EmailMessage { to: string; subject: string; text: string; }
export interface EmailProvider { send(message: EmailMessage): Promise<void>; }
export class MemoryEmailProvider implements EmailProvider { readonly messages: EmailMessage[] = []; async send(message: EmailMessage) { this.messages.push(structuredClone(message)); } }
export interface AvatarStorageProvider { get(key: string): Promise<{ bytes: Uint8Array; contentType: string } | null>; put(key: string, bytes: Uint8Array, contentType: string): Promise<void>; delete(key: string): Promise<void>; }
export class MemoryAvatarStorage implements AvatarStorageProvider { readonly files = new Map<string, { bytes: Uint8Array; contentType: string }>(); async get(key: string) { const file = this.files.get(key); return file ? { bytes: file.bytes.slice(), contentType: file.contentType } : null; } async put(key: string, bytes: Uint8Array, contentType: string) { this.files.set(key, { bytes: bytes.slice(), contentType }); } async delete(key: string) { this.files.delete(key); } }
export function assertNonProductionProvider(nodeEnv: string, provider: 'memory' | 'none') { if (nodeEnv === 'production' && provider === 'memory') throw new Error('In-memory provider is forbidden in production'); }
